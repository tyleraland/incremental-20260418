node_modules
dist
dist-ssr
*.local
.DS_Store

# Playwright e2e output (perf harness)
playwright-report
test-results
e2e/__shots__

# Cached battle-snapshot tokens (scripts/bsnap.mjs)
.bsnap

# Codemap viewer built into public/ for local dev (npm run codemap); prod builds it in CI
/public/codemap

# Test coverage output (consumed by tools/codemap; regenerated by npm run coverage)
/coverage

# Sub-agent isolated worktrees (session-local, never project content)
/.claude/worktrees/
